package dev.nekorush14.kutimu.data

/**
 *  An enum class to define category of tasks.
 */
enum class TaskCategory {
    Inbox, Private, Work
}

/**
 *  An enum class to define frequency of tasks.
 */
enum class TaskFrequency {
    Once, Daily, Weekly, Monthly, Yearly
}

/**
 *  An enum class to define category of habits.
 */
enum class HabitCategory {
    Inbox, Task, Hobby, Important
}

/**
 *  An enum class to define frequency of habits.
 */
enum class HabitFrequency {
    Daily, Weekly, Monthly, Yearly
}